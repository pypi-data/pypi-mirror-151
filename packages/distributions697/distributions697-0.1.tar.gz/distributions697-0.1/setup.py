from setuptools import setup

setup(name='distributions697',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions697'],
      author = 'Alexander Hepel',
      zip_safe=False)
