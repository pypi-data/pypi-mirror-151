[project]
name = "pdm-self"
version = "0.1.0"
description = ""
authors = [
    { name = "Charlie Bini", email = "5003326+cbini@users.noreply.github.com" },
]
dependencies = [
    "pdm>=1.15.0",
]
requires-python = ">=3.9"
readme = "README.md"

[project.license]
text = "MIT"

[project.optional-dependencies]

[project.entry-points.pdm]
pdm-self = "pdm_self:main"

[tool.pdm.dev-dependencies]
dev = [
    "black>=22.3.0",
    "flake8>=4.0.1",
]

[build-system]
requires = [
    "pdm-pep517>=0.12.0",
]
build-backend = "pdm.pep517.api"
